<table border="0" cellpadding="0" cellspacing="0" width="100%">
  <tr class="hole_red">
    <!-- PAR -->
    <td>
      <%= format_nil(hole.length_red,'0') -%>
    </td>
  </tr>
  <tr class="hole_yellow">
    <!-- PAR -->
    <td>
      <%= format_nil(hole.length_yellow,'0') -%>
    </td>
  </tr>
  <tr class="hole_blue">
    <!-- PAR -->
    <td>
      <%= format_nil(hole.length_blue,'0') -%>
    </td>
  </tr>
  <tr class="hole_white">
    <!-- PAR -->
    <td>
      <%= format_nil(hole.length_white,'0') -%>
    </td>
  </tr>
  <tr>
    <!-- PAR -->
    <td>
      <%= format_nil(hole.par,'0') -%>
    </td>
  </tr>
  <tr>
    <!-- PAR -->
    <td>
      <%= format_nil(hole.handicap,'0') -%>
    </td>
  </tr>
</table>
