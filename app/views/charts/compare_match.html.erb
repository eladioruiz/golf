
                
                <%
                # The xml is obtained as a string from builder template.
                str_xml = render "calculate_compare_match.builder",{:match => @match, :players => @players, :holes => @holes}

                #Create the chart - LIne Chart with data from strXML MultiSerie
                render_chart '/chart/FCF_MSLine.swf', '', str_xml, 'CompareStrokes', 600, 400, false, false do-%>
                <% end-%>
