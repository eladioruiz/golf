<%
#In this example, we show how to connect FusionCharts to a database.
#For the sake of ease, we've used a database which contains two tables, which are linked to each
#other.
# The xml is obtained as a string from builder template.
str_xml = render "calculate_pie_courses.builder",{:matches_data => @matches_data}

#Create the chart - Pie 3D Chart with data from strXML
render_chart '/chart/FCF_Pie3D.swf', '', str_xml, 'CoursesPlayedSum', 400, 250, false, false do-%>
<% end-%>
